# Generated TLS certs / private keys — regenerated locally via generate-indexer-certs.yml
wazuh/single-node/config/wazuh_indexer_ssl_certs/

# VM disks (VHDX/AVHDX) — several GB, never versioned
vms/**/*.vhdx
vms/**/*.avhdx

# OS / editor cruft
.DS_Store
Thumbs.db
